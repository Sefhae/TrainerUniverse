export async function register() {
  // The database schema now lives in Supabase and is created once by running
  // supabase/schema.sql in the Supabase SQL Editor (see SUPABASE_SETUP.md).
  // Nothing to provision at server boot anymore.
}
